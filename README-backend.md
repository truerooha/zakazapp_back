## Backend (Node + Express + SQLite)

- Старт dev-сервера:

```bash
cd backend
npm install
npm run dev
```

- Базовый URL по умолчанию: `http://localhost:4000`

Фронтенд читает настройки из `VITE_API_URL` (например, `http://localhost:4000`), иначе использует URL по умолчанию.


